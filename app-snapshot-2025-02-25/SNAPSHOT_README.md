# Nautical Ops – App Snapshot (Feb 25, 2025)

This folder is a snapshot of the Yachy / Nautical Ops app as it looked and functioned on **February 25, 2025** — the version you're happy with.

## To Run

```bash
cd "Yachy App/app-snapshot-2025-02-25"
npm install
npx expo start
```

*(From your project root, or use the full path: `/Users/clintonhandford/Desktop/Yachy App/app-snapshot-2025-02-25`)*

## What's Included

- Full source code (`src/`)
- Assets (logo, icons, etc.)
- Configuration (`package.json`, `app.json`, `tsconfig.json`)
- Supabase migrations and config
- Documentation and SQL scripts

**Excluded:** `node_modules`, `.expo`, `dist`, `.git` (run `npm install` to restore deps)
