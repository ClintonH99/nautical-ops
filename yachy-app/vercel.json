{
  "buildCommand": "node scripts/inject-pricing-config.mjs && npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/pricing", "destination": "/pricing.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
